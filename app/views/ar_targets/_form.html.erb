<%= form_for(@ar_target) do |f| %>
  <% if @ar_target.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ar_target.errors.count, "error") %> prohibited this ar_target from being saved:</h2>

      <ul>
      <% @ar_target.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label :data_url %><br>
    <%= f.text_field :data_url %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
